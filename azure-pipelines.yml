# Docker
# Build a Docker image 
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

steps:
-  script:
     docker -f Dockerfile build -t sfdeploy:test1 .
     docker run sfdeploy:test